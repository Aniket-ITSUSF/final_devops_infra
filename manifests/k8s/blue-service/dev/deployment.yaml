apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-service
  labels:
    app: blue-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blue-service
  template:
    metadata:
      labels:
        app: blue-service
    spec:
      containers:
        - name: blue-service
          image: 653572728575.dkr.ecr.us-east-1.amazonaws.com/blue-service:develop
          ports:
            - containerPort: 3000
          env:
            - name: DB_HOST
              value: ${{DB_HOST}}
            - name: DB_PORT
              value: ${{DB_PORT}}
            - name: DB_USER
              value: ${{DB_USER}}
            - name: DB_PASS
              value: ${{DB_PASS}}
            - name: DB_NAME
              value: ${{DB_NAME}}
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
