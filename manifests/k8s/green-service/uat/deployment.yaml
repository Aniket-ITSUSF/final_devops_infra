apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-service
  labels:
    app: green-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: green-service
  template:
    metadata:
      labels:
        app: green-service
    spec:
      containers:
        - name: green-service
          image: ${{AWS_ACCOUNT_ID}}.dkr.ecr.${{AWS_REGION}}.amazonaws.com/green-service:uat
          ports:
            - containerPort: 3000
          env:
            - name: DB_HOST
              value: ${{DB_HOST}}
            - name: DB_PORT
              value: ${{DB_PORT}}
            - name: DB_USER
              value: ${{DB_USER}}
            - name: DB_PASS
              value: ${{DB_PASS}}
            - name: DB_NAME
              value: ${{DB_NAME}}
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
