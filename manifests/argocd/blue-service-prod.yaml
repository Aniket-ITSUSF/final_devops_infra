apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: blue-service-prod
  namespace: argocd
spec:
  project: default

  source:
    repoURL: 'https://github.com/Aniket-ITSUSF/final_devops_infra.git'
    targetRevision: main
    path: 'manifests/helm-charts/blue-service'
    helm:
      valueFiles:
        - values-prod.yaml

  destination:
    name: prod
    namespace: prod

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
